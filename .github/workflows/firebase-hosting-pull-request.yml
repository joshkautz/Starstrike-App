# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# Reference:
# "Deploy to Firebase Hosting" GitHub Action: https://github.com/marketplace/actions/deploy-to-firebase-hosting

name: Deploy to Firebase Hosting Preview Channel

on: pull_request

permissions: write-all

jobs:
  job:
    name: Checkout and Deploy to Firebase Hosting Preview Channel
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Deploy to Firebase Hosting Preview Channel
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_STARSTRUCK_GITHUB_APP_BEE1B }}
          projectId: starstruck-github-app-bee1b
